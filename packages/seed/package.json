{
  "name": "@peel/seed",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "db-seed": "pnpm with-env ts-node --transpile-only index.ts",
    "with-env": "dotenv -e ../../.env --",
    "clean": "rm -rf .turbo node_modules",
    "lint": "eslint . --ext .ts,.tsx",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@peel/api": "*"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "dotenv-cli": "^6.0.0",
    "eslint": "^8.28.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.3"
  }
}
